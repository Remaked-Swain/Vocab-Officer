# RUN-20260526-CL0005: Selected-Set Testing, Study Cards And Installation

| Field | Value |
| --- | --- |
| Status | completed |
| Recorded at | 2026-05-26 (Asia/Seoul) |
| Goal | Restore testing for older untested sets, prevent test launch crashes, add study cards and install the local app |
| Prior decisions loaded | CL-0001, CL-0002, CL-0003, CL-0004, CL-0005 |
| Roles | Director, Executor, Monitor, Recorder |
| Payload retention | No real vocabulary payload or raw crash report is copied into this record |
| Archive review | Retain while this run evidences the defect repair and local installation |

## Changes

- Add an explicit selected-set test mode so an older completed set remains
  testable after a newer set has been entered.
- Prevent candidate-free session persistence and present session plus questions
  atomically, eliminating the empty question-array crash path.
- Add a set-grouped learning-card screen whose cards flip between headword and
  meanings.
- Make the application grading path accept any independently stored Korean meaning and show
  headword, full registered meanings and submitted answer during correct or
  incorrect result acknowledgement.
- Add a Release install-and-verify path for `/Applications/Vocab.app`.

## Verification

| Check | Result |
| --- | --- |
| Persisted-state diagnosis | confirmed two completed 100-word sets exist locally without copying their payload into Git evidence |
| Crash diagnosis | confirmed empty question-array subscript during test-sheet rendering; fixed by atomic run payload, safe optional rendering and candidate-free session rejection |
| Selected-set regression | passed; a synthetic older untested set remains selectable after a newer set exists and sessions on different Seoul dates still prioritize words never previously presented |
| Grading regression | passed; an application-path synthetic word with multiple Korean meanings accepts a non-first registered meaning automatically |
| Meaning-progress integrity | passed; a delimited legacy meaning cannot earn automatic or corrected mastery credit, while independently stored meanings remain eligible |
| Empty-candidate regression | passed; an empty review pool throws without persisting an empty test session |
| Full Debug XCTest | passed; 22 tests |
| XCTest result bundle | `test-build/Logs/Test/Test-Vocab-2026.05.26_19-51-55-+0900.xcresult` |
| Debug presentation build | passed through impact-selected verification |
| Release installation and launch | passed; `/Applications/Vocab.app` exists and reports bundle identifier `com.swainyun.Vocab` |
| Closed-Loop ledger validation | passed; 9 indexed records |
| Verification selection self-test | passed |
| Repository payload/privacy scan | passed; no user vocabulary payload, local store path or credential-like value was introduced into tracked evidence |

Interactive card flipping and acknowledgement-panel visual inspection were
attempted through installed-app accessibility inspection, but macOS denied
assistive access to `osascript`. They remain explicit manual macOS UI checks;
no raw screen capture or real vocabulary payload is retained in this record.

## Closure

- Director approved closure after confirming selected-set history priority and
  legacy combined-meaning mastery protection.
- Monitor approved closure and accepted the recorded manual UI verification
  residual caused by macOS assistive-access denial.
- Recorder approved durable retention of `CL-0005` and completion of this run
  after ledger synchronization.
- Executor reported no remaining implementation blocker.
