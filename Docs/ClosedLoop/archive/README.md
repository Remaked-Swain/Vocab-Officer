# Closed-Loop Archive

This directory holds records moved out of active lookup after the retention
rules in `../README.md` have been satisfied. Archived decisions remain indexed
when they document a migration, deletion rule, unresolved limitation or
historically relevant approval.

No record is moved here or deleted automatically. The archive move, index
update, superseding link and Monitor approval are committed together.

